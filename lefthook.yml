# Lefthook Konfiguration für roessing.de
# Dokumentation: https://github.com/evilmartians/lefthook

pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{js,ts,jsx,tsx,json,astro}"
      run: npx biome check --write {staged_files} && git add {staged_files}
      stage_fixed: true
